let contador = 0;

while (contador <= 10){
    console.log(`Número atual: ${contador}`);
    contador++;
}

contador = 0;

do {
    console.log(`Contando: ${contador}...`);
    contador++
} while (contador <= 10);